# Update

Apply changes only when explicitly authorized by the user.

Before mutating:

- read the target file;
- preserve existing evidence and provenance;
- keep the representation minimal;
- avoid introducing schema that the method does not require.

After mutation, create a clear Git commit describing the substantive change.

Derived index sections, when introduced by repository tooling, must be regenerated by tooling rather than hand-edited.
